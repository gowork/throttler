{
    "name": "gowork/throttler",
    "description": "Simple PHP execution throttler.",
    "keywords": ["throttling", "throttle", "execution throttling"],
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^7.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^8.5 || ^9.0",
        "phpstan/phpstan": "^0.12.3"
    },
    "autoload": {
        "psr-4": {
            "GW\\Throttler\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "tests\\GW\\Throttler\\": "tests/"
        }
    }
}
